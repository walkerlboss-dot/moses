# ═══════════════════════════════════════════════════════════════════════════
#  Moses v3.0 — Production Dependencies
# ═══════════════════════════════════════════════════════════════════════════
# Install with:  pip install -r requirements.txt
# Or use:        pip install -e .
#
# These versions are pinned to ensure reproducible builds.
# Update via:    pip-compile pyproject.toml  (if using pip-tools)
# ═══════════════════════════════════════════════════════════════════════════

# ── Core ──────────────────────────────────────────────────────────────────
numpy>=1.24,<2.0
torch>=2.2.0
gymnasium>=0.29.0
hydra-core>=1.3.0
omegaconf>=2.3.0
wandb>=0.16.0
tensorboard>=2.15.0
tqdm>=4.66.0
rich>=13.7.0

# ── NVIDIA Isaac Lab / Omniverse ──────────────────────────────────────────
isaaclab>=0.22.0

# ── Robotics / RL libraries ───────────────────────────────────────────────
rsl-rl-lib>=2.0.0
stable-baselines3>=2.3.0

# ── Utilities ─────────────────────────────────────────────────────────────
pyyaml>=6.0.1
pydantic>=2.5.0
typer>=0.12.0
loguru>=0.7.0

# ── Optional: headless rendering / camera support ─────────────────────────
# Uncomment if cameras / RGB observations are used:
# opencv-python>=4.9.0
# imageio>=2.34.0
